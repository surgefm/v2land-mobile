import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    marginLeft: 0,
    marginRight: 0,
  },
  socialButton: {
    minWidth: 150,
  },
  textStyle: {
    fontSize: 16,
  },
});
